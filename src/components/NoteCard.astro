---
/** Render a Note summary. */
import Card from 'component:Card.astro';
import { Markdown } from 'astro/components';

const { note } = Astro.props;
---
<Card article={ note }>
  <Markdown content={ note.cardContent } />
</Card>
