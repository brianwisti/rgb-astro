---
/*** Render tags associated with an article */
import Tag from 'component:Tag.astro';

const { article } = Astro.props;
let tags = article.tags;

if (tags === undefined) {
  tags = []
}
---
{tags.map((tag) => <Tag tag={tag} />)}
