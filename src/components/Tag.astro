---
/*** Render a tag as a link to a list of tagged articles */

const { tag } = Astro.props;
const tagHref = `/tag/${ tag }`;
---
<a href={ tagHref } set:html={ tag } />

<style>
a::before {
  content: "#";
}
</style>
